LDADD +=	-lz -lutil
PREFIX ?=	/usr/local
BINDIR ?=	${PREFIX}/sbin
LIBEXECDIR ?=	${PREFIX}/libexec
CHROOT_DIR ?=	/var/www
HTTPD_DIR =	${CHROOT_DIR}/htdocs
PROG_DIR =	${HTTPD_DIR}/${PROG}
WWWUSR ?=	www
WWWGRP ?=	www
